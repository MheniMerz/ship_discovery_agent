Value NAME (\w+)
Value MACADDR (([a-z0-9]{2}\:){5}([a-z0-9]{2}))
Value IPADDR (([0-9]{3}\.){3}([0-9]{1,2}\/[0-9]{1,2}))
Value TYPE (\w+)

Start
  ^Interface ${NAME}
  ^.*HWaddr: ${MACADDR}
  ^.*inet ${IPADDR}
  ^.*Interface Type ${TYPE} -> Record